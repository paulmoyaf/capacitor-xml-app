import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'capacitor.xml.app',
  appName: 'capacitor-xml-app',
  webDir: 'dist/capacitor-xml-app',
};

export default config;
